# 自动迭代优化 - 使用说明

## 当前状态
- 当前 mAP50: **0.6479**
- 目标 mAP50: **>0.95**
- 当前 Precision: **0.6712** (目标 >0.90)
- 当前 Recall: **0.6857** (目标 >0.85)

## 项目文件

### 训练脚本
- `train.py` - 主训练脚本 (Windows GPU)
- `train_gpu.py` - 备用GPU训练脚本
- `train_with_config.py` - 配置文件驱动的通用训练脚本
- `train_windows.bat` - Windows启动脚本

### 实验配置 (3个核心实验)
- `exp01_config.py` - 实验1: yolo12m 基线
- `exp02_config.py` - 实验2: Copy-Paste增强
- `exp03_config.py` - 实验3: 小目标优化

### 批量运行
- `run_all_experiments.bat` - 一键运行所有3个实验

### 文档
- `experiment_plan.md` - 完整的实验设计和迭代路线
- `results.tsv` - 实验结果记录
- `last_metrics.json` - 最新的性能指标

---

## Windows GPU 使用指南

### 方法1: 一键运行所有实验 (推荐)

在Windows上双击或运行:
```batch
run_all_experiments.bat
```

这将按顺序运行3个实验，每个约1-2小时（取决于GPU）

### 方法2: 单独运行特定实验

```batch
# 实验1: yolo12m 基线
python train_with_config.py exp01_config.py

# 实验2: Copy-Paste增强
python train_with_config.py exp02_config.py

# 实验3: 小目标优化
python train_with_config.py exp03_config.py
```

### 方法3: 使用原始train.py

修改 `train.py` 中的参数，然后:
```batch
python train.py > run.log 2>&1
```

---

## 实验设计路线图

### 阶段1: 模型升级 (已配置)
- yolo12m (提升容量)
- 预期提升: mAP50 0.65 → ~0.75

### 阶段2: 数据增强 (已配置)
- Copy-Paste增强至0.3
- Mixup增强至0.2
- 预期提升: mAP50 0.75 → ~0.82

### 阶段3: 小目标优化 (已配置)
- 提升分类权重至0.8
- 降低定位权重至7.5
- 预期提升: mAP50 0.82 → ~0.88

### 阶段4: 超参数搜索 (下一步)
- 学习率扫描
- 损失权重扫描
- 预期提升: mAP50 0.88 → ~0.93

### 阶段5: yolo12l 大模型 (最终)
- 如果GPU内存足够，使用yolo12l
- 预期提升: mAP50 0.93 → ~0.96+

---

## 结果查看

训练完成后:
1. 查看终端输出
2. 查看 `last_metrics.json`
3. 查看 `autoresearch_runs/` 目录下的图表
4. 查看 `results.tsv` 历史记录

---

## 记录实验结果

每次训练完成后，请在 `results.tsv` 中追加:
```
实验名称\t模型\tepochs\timgsz\tmAP50\tprecision\trecall\tsmall_obj_recall\tcounting_mae\t备注
```

---

## 快速开始

**在Windows GPU机器上:**

1. 确保数据在 `D:\PythonProject\person_dataset\`
2. 确保conda环境: `conda activate ultralytics_object`
3. 运行: `run_all_experiments.bat`
4. 等待1-2天（3个实验 × 1-2小时/实验）
5. 查看结果并决定下一步

**注意:** 每个实验约1-2小时，具体取决于GPU性能。